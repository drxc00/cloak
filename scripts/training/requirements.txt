# PII token-classifier training & export — build/release-time only, never at runtime.
# Freeze exact versions so CI produces reproducible artifacts.
torch>=2.0.0,<3.0
transformers>=4.48.0,<5.0
datasets>=2.19.0
accelerate>=0.33.0
seqeval>=1.2.2
evaluate>=0.4.0
onnx>=1.16.0
onnxruntime>=1.18.0
onnxscript>=0.1
# Domain augmentation (optional, recommended)
faker>=30.0.0
# Few-NERD parquet reading
pyarrow>=14.0.0
